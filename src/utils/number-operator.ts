export const add = (num1: number) => (num2: number) => {
  return num1 + num2
}

export const minus = (num1: number) => (num2: number) => {
  return num1 - num2
}

export const print = () => {
  console.log('print ja')
}
